For running on windows, running `setup.ps1` should install and configure MongoDB as needed.

Linux does not have an automated script as of yet, since we host a lot on a raspberry pi which needs custom third-party MongoDB binaries.